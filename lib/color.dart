import 'package:flutter/material.dart';

class ColorText extends StatelessWidget {
  final String mainText;
   ColorText(this.mainText);

  @override
  Widget build(BuildContext context) {

    return Text(mainText);
  }
}
